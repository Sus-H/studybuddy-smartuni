services:
  studybuddy:
    build: ./StudyBuddy Studio (latest version)
    container_name: studybuddy-studio-app
    restart: unless-stopped
    environment:
      # Streamlit Configuration
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_SERVER_ENABLE_CORS=false
      - STREAMLIT_SERVER_ENABLE_XSRF_PROTECTION=false
      # External Neo4j connection (Chalmers)
      - NEO4J_URI=${NEO4J_URI}
      - NEO4J_USER=${NEO4J_USERNAME}
      - NEO4J_PASSWORD=${NEO4J_PASSWORD}
      # LiteLLM Configuration
      - LITELLM_API_KEY=${LITELLM_API_KEY}
      - LITELLM_BASE_URL=${LITELLM_BASE_URL}
      - LITELLM_MODEL=${LITELLM_MODEL}
      # Optional: Other API keys
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - ./StudyBuddy Studio (latest version)/data:/app/data
      - ./StudyBuddy Studio (latest version)/uploads:/app/uploads
    labels:
      - "traefik.enable=true"
      # HTTP
      - "traefik.http.routers.studybuddy.rule=Host(`studybuddy-studio.sommarprojekt.chalmers.se`)"
      - "traefik.http.routers.studybuddy.entrypoints=web"
      - "traefik.http.services.studybuddy.loadbalancer.server.port=8501"
      - "traefik.http.routers.studybuddy.middlewares=redirect-to-https,chalmers-ipwhitelist"
      # HTTPS with SSL
      - "traefik.http.routers.studybuddy-secure.rule=Host(`studybuddy-studio.sommarprojekt.chalmers.se`)"
      - "traefik.http.routers.studybuddy-secure.entrypoints=websecure"
      - "traefik.http.routers.studybuddy-secure.tls.certresolver=letsencrypt"
      - "traefik.http.routers.studybuddy-secure.middlewares=chalmers-ipwhitelist"
      # Redirect HTTP to HTTPS middleware
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
    networks:
      - traefik

networks:
  traefik:
    name: traefik
    external: true